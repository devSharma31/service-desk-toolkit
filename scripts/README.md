# Scripts

PowerShell and Bash helpers for L1/L2 support.

- `Check-Connectivity.ps1` – Pings key endpoints and prints a simple health summary.
- `M365-User-Audit.ps1` – Lists users with license + MFA info (sample-only).
- `check_dns.sh` – Quick DNS + curl checks for Linux endpoints.
